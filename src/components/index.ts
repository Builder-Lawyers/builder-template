import type { HeaderProps } from "@/components/header/Header.types.ts";
import type { HeroProps } from "@/components/hero/Hero.types.ts";

export type WidgetMap = {
  header: HeaderProps;
  hero: HeroProps;
};
